{
  "name": "demo-landing",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "GStack + Superpowers 풀 워크플로우 데모 — 정적 랜딩 페이지",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
